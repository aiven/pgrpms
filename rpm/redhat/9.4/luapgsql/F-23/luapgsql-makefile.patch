--- GNUmakefile.old	2016-08-13 09:27:46.000000000 +0200
+++ GNUmakefile	2016-08-23 15:54:44.581870267 +0200
@@ -4,12 +4,12 @@
 LUAVER=		$(shell lua -v 2>&1 | cut -c 5-7)
 
 CFLAGS+=	-O3 -Wall -fPIC -I/usr/include -I/usr/include/lua${LUAVER} \
-		-I/usr/include/postgresql
+		-I$(shell /usr/pgsql-9.4/bin/pg_config --includedir)
 
-LDADD+=		-L/usr/lib -lpq
+LDADD+=		-L$(shell /usr/pgsql-9.4/bin/pg_config --libdir) -lpq
 
 LIBDIR=		/usr/lib
-LUADIR=		/usr/lib/lua/${LUAVER}
+LUADIR=		/usr/lib64/lua/${LUAVER}
 
 ${LIB}.so:	${SRCS:.c=.o} luapgsql.h
 		cc -shared -o ${LIB}.so ${CFLAGS} ${SRCS:.c=.o} ${LDADD}
@@ -17,5 +17,5 @@
 clean:
 		rm -f *.o *.so
 install:
-	install -d ${DESTDIR}${LIBDIR}
+	install -d ${DESTDIR}${LUADIR}
 	install -m 755 ${LIB}.so ${DESTDIR}${LUADIR}/${LIB}.so
